# The PocketBase binary is downloaded per-machine by scripts/setup.sh —
# never commit it. (Different OS/arch per machine, and it's ~40 MB.)
pocketbase
pocketbase.exe
pocketbase_*.zip

# Live database, uploaded files, and logs. This is the self-hoster's data,
# not source code. The schema lives in pb_migrations/ (which IS committed).
pb_data/

# Throwaway database created and deleted by scripts/smoke_test.sh.
pb_data_smoke/

# OS noise
.DS_Store
Thumbs.db

# Local editor/agent tooling config
.claude/

# Local seed scripts containing REAL division data (employee names, client
# job codes, odometer readings) — this is a public repo; office data stays
# out of it. The committed seed_demo.sh stays fake-named.
scripts/seed_local_*
